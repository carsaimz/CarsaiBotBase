<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark">

    <!-- Home screen -->
    <include android:id="@+id/home_view" layout="@layout/view_home"/>

    <!-- Browser screen -->
    <include android:id="@+id/browser_view" layout="@layout/view_browser"
        android:visibility="gone"/>

    <!-- Tabs bottom sheet anchor — handled programmatically -->
</FrameLayout>
